// The full icon source stays in `prod-icons/` at the project root so it can
// also be packaged or previewed independently. Re-export it through the app's
// existing icon namespace for normal Ignite imports.
export * from "../../../prod-icons/base/icons"
